query Posts($username: String!) {
  user(username: $username) {
    name
    coverImage
    publication {
      title
      posts {
        _id
        title
        dateAdded
        coverImage
        totalReactions
        responseCount
      }
    }
  }
}
