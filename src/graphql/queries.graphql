query Posts($username: String!) {
  user(username: $username) {
    name
    publication {
      title
      posts {
        _id
        title
        dateAdded
        coverImage
        totalReactions
        responseCount
      }
    }
  }
}
